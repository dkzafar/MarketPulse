Please perform a full system validation of our trading analytics platform:

1. **Backend Endpoint Verification**  
   • Confirm GET /api/analysis/:symbol exists and returns real OHLCV data, computed SMA50/200/RSI/BollingerBands, top-5 NewsAPI headlines with sentiment, and a structured AI summary (BUY/HOLD/SELL signal with confidence, risk profile, entry/exit levels, 3 trade ideas).  
   • Confirm GET /api/demo-enhanced/:symbol exists and returns the above plus backtest metrics (total return %, annualized return %, Sharpe ratio, max drawdown %, win rate) and risk metrics (30/60-day vol, parametric & Monte Carlo VaR, drawdown stats).  
   • If you have portfolio endpoints (e.g. /api/portfolio-optimization), confirm they return efficient-frontier weights for a real sample multi-asset portfolio.  
   Verify that **all** handlers pull from real, free data sources (CoinGecko/AlphaVantage, NewsAPI, Hugging Face inference) and use mathematically correct formulas—no dummy/demo data or placeholder values.

2. **Frontend Integration & Performance**  
   • Simulate a user clicking on symbols (e.g. BTC, AAPL, EURUSD). For each:  
     – Fetch from /api/analysis/:symbol and render **all** fields (no missing or placeholder text).  
     – Measure UI render time (< 500 ms) and check for console errors or layout shifts.  
   • Test the “Demo Enhanced” view for the same symbols via /api/demo-enhanced/:symbol, ensuring the backtest and risk tables plot correctly and interact smoothly.

3. **Latency & Resource Checks**  
   • Measure each backend call: should complete in under 3 s (Hugging Face calls welcome but under 5 s).  
   • Verify parallel fetches (news + TA) are used where applicable, and no endpoint blocks the event loop.

4. **Unit & Integration Tests**  
   • Run Jest tests for backtest.ts, risk.ts, ta.ts.  
   • If you have forecast.py, run it on a small sample CSV and confirm it outputs four forecast series in forecasts.csv.  
   Report any failures or missing test coverage.

5. **Environment & Security**  
   • Confirm environment variables (HUGGINGFACE_TOKEN, NEWSAPI_KEY) are loaded and no API keys are hard-coded.  
   • Ensure rate-limits are respected (no 429 errors).

Provide a **PASS/FAIL** summary at the end with detailed errors for any missing/mismatched endpoints, data issues, performance lags, test failures, or security lapses.  